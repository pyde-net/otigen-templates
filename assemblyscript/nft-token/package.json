{
  "name": "pyde-template",
  "version": "0.1.0",
  "description": "PTS NFT collection (pts-n/1) with per-id approvals and on-chain metadata.",
  "private": true,
  "scripts": {
    "build": "asc assembly/index.ts --config asconfig.json --target release"
  },
  "dependencies": {
    "@pyde-net/host": "0.1.0-alpha.16"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.0"
  }
}
