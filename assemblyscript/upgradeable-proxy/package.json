{
  "name": "pyde-template",
  "version": "0.1.0",
  "description": "Thin proxy that delegate-calls into a swappable logic contract.",
  "private": true,
  "scripts": {
    "build": "asc assembly/index.ts --config asconfig.json --target release"
  },
  "dependencies": {
    "@pyde-net/host": "0.1.0-alpha.18"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.0"
  }
}
