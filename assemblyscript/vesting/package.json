{
  "name": "pyde-template",
  "version": "0.1.0",
  "description": "Linear vesting with cliff: the canonical Pyde time-locked allocation example.",
  "private": true,
  "scripts": {
    "build": "asc assembly/index.ts --config asconfig.json --target release"
  },
  "dependencies": {
    "@pyde-net/host": "0.1.0-alpha.18"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.0"
  }
}
